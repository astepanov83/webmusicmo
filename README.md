# webmusicmo

A small, dark web player for internet radio: METAL ONLY and the 46 SomaFM
channels. Pick a station on the left. Built to live in a RamBox tab. No
dependencies, no build step.

![player](docs/screenshot.png)

## Run it

```sh
npm start                # http://127.0.0.1:8420
PORT=9000 npm start      # pick another port
npm run refresh          # rebuild public/stations.json from stations/local.json and SomaFM
npm test
```

Environment:

| Variable              | Default                          | Meaning                                       |
| ---------------------- | --------------------------------- | ---------------------------------------------- |
| `PORT`                | `8420`                            | Port to listen on                              |
| `HOST`                | `127.0.0.1`                       | Bind address (`0.0.0.0` for LAN)               |
| `STATIONS_FILE`       | `public/stations.json`            | Station list the server reads and the refresh script writes |
| `SOMAFM_URL`          | `https://somafm.com/channels.json`| Where the refresh script fetches SomaFM's channel list |
| `LOCAL_STATIONS_FILE` | `stations/local.json`             | Hand-written stations the refresh script starts from |
| `REFRESH_HOURS`       | `24`                              | Docker only: how often the container refreshes the station list. `0` turns it off |

`REFRESH_HOURS` must be a whole number of hours. Do not write it with a
leading zero: the shell reads a leading zero as base eight, so `010` means 8
hours, not 10.

## Run it as a service

```sh
scripts/install-service.sh          # picks the first free port from 8420 up
scripts/install-service.sh 9000     # or choose one
scripts/uninstall-service.sh
```

The install script writes a systemd user unit to `~/.config/systemd/user/webmusicmo.service`,
enables it and starts it. It restarts on failure and comes back after reboot.
It also installs `webmusicmo-refresh.timer`, which runs the refresh script
once a day and shortly after boot if a run was missed. Check on it with:

```sh
systemctl --user status webmusicmo
journalctl --user -u webmusicmo -f
systemctl --user list-timers webmusicmo-refresh.timer
journalctl --user -u webmusicmo-refresh -f
```

## Stations

`stations/local.json` holds the hand-written stations. Today it holds one:

```json
{
  "id": "metal-only",
  "name": "Metal Only",
  "description": "Metal, 24 hours a day, with live shows by real DJs. metal-only.de",
  "genre": "metal",
  "dj": "",
  "site": "https://www.metal-only.de",
  "logo": "/icon-256.png",
  "source": "local",
  "playlists": [
    { "url": "https://metal-only.streampanel.cloud/listen.pls", "format": "mp3", "quality": "highest" }
  ],
  "nowPlaying": { "kind": "icecast" }
}
```

| Field                | Meaning                                                        |
| --------------------- | --------------------------------------------------------------- |
| `id`                  | Short, unique, used in the URL and in storage keys              |
| `name`                | Shown in the station list and the header                        |
| `description`         | Shown under the name                                             |
| `genre`               | Shown as a tag                                                  |
| `dj`                  | Shown when the station names a host                             |
| `site`                | Link to the station's own page                                  |
| `logo`                | Artwork shown in the list and, where it exists, on the lock screen |
| `source`              | `local` or `somafm`                                              |
| `playlists[].url`     | A PLS playlist to resolve into real stream URLs                 |
| `playlists[].format`  | `mp3` or `aac`                                                  |
| `playlists[].quality` | As given by the source, informational only                      |
| `nowPlaying.kind`     | `icecast` (read the mount's `status-json.xsl`) or `somafm` (read SomaFM's songs feed) |
| `nowPlaying.url`      | Only for `somafm`: the songs feed to read                       |

`public/stations.json` is generated by `scripts/refresh.js`. It reads
`stations/local.json`, fetches SomaFM's public `channels.json`, and writes
the merged list with the local stations first. SomaFM entries are generated,
not hand-written. If SomaFM cannot be reached, refresh keeps whatever station
list is already on disk rather than replacing a working list with a broken
one.

Local entries always come first, so the first one is the default station:
today that is METAL ONLY. To add a station, add an entry to
`stations/local.json` and run `npm run refresh`.

## What the server does

- Serves the player from `public/`.
- `GET /api/stations` lists the stations with their display fields, no
  playlists. Answers 503 if the station list cannot be read.
- `GET /api/streams?station=<id>` resolves that station's playlists, probes
  each one for its real bitrate and codec, and returns the stream list, the
  backup servers, and the playlist that actually resolved. Answers 404 for
  an unknown station and 502 if no playlist could be read.
- `GET /api/now?station=<id>` returns the current track plus listeners and a
  recent-tracks list. The list is empty for an Icecast station; for a
  SomaFM station it holds roughly the last twenty tracks, and seeds the
  recently-played panel.
- `GET /api/health` for a liveness check.

Without `station`, the first station in the list is used, which is METAL ONLY.

Audio goes straight from the browser to the station. The server only hands
out URLs and metadata.

## Player features

- Station list on the left, with a filter box. On a phone the list opens as
  a full-screen overlay from the station name in the header. `[` and `]`
  step through stations, `/` jumps to the filter.
- Play / stop that always rejoins the live edge.
- Volume with a perceptual curve, mute, both remembered.
- Quality picker: whatever qualities the chosen station advertises, highest
  first. Remembered per station.
- Now playing with DJ and show, plus a history of recent tracks. Remembered
  per station.
- `?station=<id>` in the address opens that station, and the address updates
  as you switch.
- Phone layout: one column with the history below the player, bigger touch
  targets, no fixed heights.
- Spectrum ring around the play button: log-spaced bands, fast attack and
  slow release, peak caps, bass-driven glow (toggle with `v`).
- Sleep timer with a slow fade-out.
- Auto-reconnect with backoff, and a switch to the backup servers if one
  host dies.
- Lock screen and media notification through the Media Session API: the
  station's own logo as artwork where it has one, otherwise the app icon
  (`public/icon-512.png`, `icon-256.png`). Song as title, artist as
  subtitle, play and pause. No previous/next, it is a live stream.
- Phone calls and other audio interruptions: when the browser pauses the
  player, the app shows "Paused" and does not reconnect on its own. No
  retry and no stall timer runs until play is pressed again (on the page,
  the keyboard, or the lock screen) or the browser hands the audio back
  after the call. Every resume rejoins the live edge with a fresh connection.
- `?debug=1` exposes `window.__player` (audio element, state, AudioContext)
  for testing.
- Theme toggle: dark, light, or follow the system (`t`).
- Keyboard: `Space` play/stop, `M` mute, arrows for volume, `[` `]` step
  stations, `/` filter stations.

## Docker

```sh
./docker_build.sh                 # build and push gitea.gitpal.ru/alex/webmusicmo
docker run -p 8420:8420 gitea.gitpal.ru/alex/webmusicmo
docker run -p 8420:8420 -e REFRESH_HOURS=0 gitea.gitpal.ru/alex/webmusicmo   # no refresh loop
```

The container refreshes the station list in the background every
`REFRESH_HOURS` (default 24) and then serves `public/`. `REFRESH_HOURS=0`
turns the refresh loop off and just serves the list baked into the image.
